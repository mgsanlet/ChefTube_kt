package com.mgsanlet.cheftube.domain.util.error

/**
 * Interfaz base para todos los errores del dominio.
 * Todas las clases de error específicas del dominio deben implementar esta interfaz.
 *
 * Esta interfaz se utiliza como marcador para garantizar la consistencia en el manejo
 * de errores en toda la capa de dominio.
 */
interface DomainError
